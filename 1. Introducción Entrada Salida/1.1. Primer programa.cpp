// Primer programa en C++

#include <iostream> // Incluimos el header básico de input/output stream

using namespace std; // Todo lo que viene de la biblioteca estándar está dentro del namespace std

int main(){ // main() Es nuestra función principal
    cout<<"Hola Mundo :D\n"<<endl; // El '\n' y 'endl' son saltos de línea en C++

    // Una función de tipo entero debe retornar algo para que el programa saber que finalizó
    return 0; // Es por eso que se ocupa por convención el return 0;
}
