#include <iostream>
#include "Estatico.h" // Incluimos la definición de la clase Estatico
using namespace std;

int Estatico::contador = 0; // Inicializamos la variable static contador (se define una sola vez fuera de la clase)